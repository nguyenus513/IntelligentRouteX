{
  "name": "routefood-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && npx firebase emulators:start --only functions,firestore,database,auth,storage --config ../firebase/firebase.json",
    "lint": "tsc --noEmit",
    "seed:demo": "tsx src/seed/seedHcmDemo.ts"
  },
  "dependencies": {
    "firebase-admin": "^13.8.0",
    "firebase-functions": "^7.2.5"
  },
  "devDependencies": {
    "firebase-tools": "^15.15.0",
    "tsx": "^4.21.0",
    "typescript": "^5.7.3"
  },
  "overrides": {
    "uuid": "14.0.0",
    "@tootallnate/once": "3.0.1"
  }
}
