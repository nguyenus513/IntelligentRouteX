# Dispatch V2 Large Scale Protocol

The large-scale protocol defines controlled local-real benchmark execution.

controlled mode is the assertion truth for deterministic CI checks. local-real mode is allowed for authority-machine validation.
